services:
  spark-app:
    build:
      context: .
      target: app
    container_name: bees-spark-app
    volumes:
      - ./:/app
    working_dir: /app
    command: luigi --module app.orchestration.luigi_pipeline BreweryMedallionPipeline --local-scheduler
